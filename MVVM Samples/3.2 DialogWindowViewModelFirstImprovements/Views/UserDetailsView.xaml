<UserControl x:Class="_3._2_DialogWindowViewModelFirstImprovements.Views.UserDetailsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModels="clr-namespace:_3._2_DialogWindowViewModelFirstImprovements.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             d:DataContext="{d:DesignInstance Type=viewModels:UserDetailsViewModel, IsDesignTimeCreatable=True}">

    <StackPanel Orientation="Vertical">
        <TextBlock Text="{Binding User.FirstName}" />
        <TextBlock Text="{Binding User.LastName}" />
    </StackPanel>
    
</UserControl>
